postgres_project:
  outputs:
    dev:
      dbname: airflow
      host: postgres
      pass: postgres
      port: 5432
      schema: model
      threads: 1
      type: postgres
      user: postgres
  target: dev
